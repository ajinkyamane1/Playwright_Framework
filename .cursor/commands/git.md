First check the git status, then git add each file separately. Add each file with a standard commit format.

git add <file_path> && git commit -m "<commit_message>"

afte this check again git statu is there any changes need to add